#Loadable properties file for parameter-izing Spring configuration.
#
# Put a property here e.g. "default.destination.name", and then in the Spring context
# xml config for JmsTemplate, you can e.g. set defaultDestination equal to 
# ${default.destination.name} 